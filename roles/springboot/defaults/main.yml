---
# defaults file for springboot
sb_app_path: /opt/Ap12550
sb_app_name: myservice
sb_deploy_folder: "{{ sb_app_path }}/{{ sb_app_name }}"
sb_conf_folder: "{{ sb_app_path }}/conf"

#TODO verify that configuration path can stay into springboot module
app_folder: "/opt/Ap12550"
conf_folder: "conf"
app_conf_folder: "{{ app_folder }}/{{ conf_folder }}"

service_user:  nclaudon
service_group: everyone